# Layer 0: Discovery metadata (always in memory, <1KB)
id: "_reference/minimal"
version: "1.0.0"
name: "Minimal Reference Skill"
description: "Demonstrates progressive disclosure pattern"

category: system
tags: [reference, example, minimal]
level: basic

load_weight: light
entry_point: "skill.py:MinimalSkill"

requires: []

capabilities:
  - capability_id: "echo"
    description: "Echo input with transformation"
  - capability_id: "validate"
    description: "Validate input against rules"

permissions:
  filesystem: none
  network: []
  shell: none
