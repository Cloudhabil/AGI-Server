{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "enterprise/continual-knowledge-manager",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["ingest", "preserve"]},
      "source": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"}
    }
  }
}
