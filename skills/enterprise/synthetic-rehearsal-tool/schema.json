{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "enterprise/synthetic-rehearsal-tool",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["generate", "evaluate"]},
      "corpus": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"}
    }
  }
}
