{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "enterprise/organizational-semantic-id-mapper",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["generate", "interpret"]},
      "pattern": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"}
    }
  }
}
