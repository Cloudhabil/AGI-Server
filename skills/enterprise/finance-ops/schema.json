{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "enterprise/finance-ops",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["forecast", "audit"]},
      "scope": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"},
      "checklist": {"type": "array"}
    }
  }
}
