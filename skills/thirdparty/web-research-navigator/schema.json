{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "thirdparty/web-research-navigator",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["plan", "capture"]},
      "topic": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"}
    }
  }
}
