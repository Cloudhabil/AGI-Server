id: thirdparty/browserbase-stagehand
name: Third-Party Browserbase Stagehand
version: "1.0.0"
description: Provider-specific browser navigation and recovery steps.
category: integration
level: intermediate

tags:
  - browser
  - stagehand
  - provider

dependencies: []

capabilities:
  - capability_id: navigate
    description: Build provider-specific navigation steps
  - capability_id: recover
    description: Outline recovery steps when navigation fails

permissions:
  filesystem: read
  network: [web]
  shell: none
