id: thirdparty/external-index-connector
name: External Index Connector
version: "1.0.0"
description: Orchestrate retrieval across external indexes via MCP.
category: integration
level: intermediate

tags:
  - retrieval
  - mcp
  - external

dependencies: []

capabilities:
  - capability_id: connect
    description: Outline external index connection steps
  - capability_id: query
    description: Outline external query orchestration steps

permissions:
  filesystem: read
  network: [mcp]
  shell: none
