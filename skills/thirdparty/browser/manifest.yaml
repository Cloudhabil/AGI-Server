id: thirdparty/browser
name: Third-Party Browser
version: "1.0.0"
description: Navigate web pages with structured steps and safe execution boundaries.
category: integration
level: intermediate

tags:
  - browser
  - web
  - navigation

dependencies: []

capabilities:
  - capability_id: plan
    description: Generate a navigation plan
  - capability_id: extract
    description: Define extraction steps for a page

permissions:
  filesystem: read
  network: [web]
  shell: none
