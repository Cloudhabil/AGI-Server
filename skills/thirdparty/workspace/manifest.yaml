id: thirdparty/workspace
name: Third-Party Workspace
version: "1.0.0"
description: Research across external workspaces with structured retrieval steps.
category: integration
level: intermediate

tags:
  - workspace
  - research
  - integration

dependencies: []

capabilities:
  - capability_id: query
    description: Build a query plan for workspace search
  - capability_id: synthesize
    description: Combine findings into a summary plan

permissions:
  filesystem: read
  network: [workspace]
  shell: none
