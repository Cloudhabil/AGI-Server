{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "skill_id": "thirdparty/mcp-connector",
  "input": {
    "type": "object",
    "properties": {
      "capability": {"type": "string", "enum": ["connect", "orchestrate"]},
      "server": {"type": "string"}
    },
    "required": ["capability"],
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "steps": {"type": "array"}
    }
  }
}
