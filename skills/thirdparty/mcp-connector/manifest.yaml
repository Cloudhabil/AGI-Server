id: thirdparty/mcp-connector
name: Third-Party MCP Connector
version: "1.0.0"
description: Plan MCP connection setup and tool orchestration steps.
category: integration
level: intermediate

tags:
  - mcp
  - connector
  - integration

dependencies: []

capabilities:
  - capability_id: connect
    description: Outline MCP connection steps
  - capability_id: orchestrate
    description: Outline tool orchestration steps

permissions:
  filesystem: read
  network: [mcp]
  shell: none
