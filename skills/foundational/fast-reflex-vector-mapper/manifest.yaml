id: foundational/fast-reflex-vector-mapper
name: Fast Reflex Vector Mapper
version: "1.0.0"
description: Encode queries into vectors and map to retrieval steps.
category: data
level: intermediate

tags:
  - retrieval
  - vectors
  - mapping

dependencies: []

capabilities:
  - capability_id: encode
    description: Outline vector encoding steps
  - capability_id: search
    description: Outline vector search steps

permissions:
  filesystem: read
  network: []
  shell: none
